You are writing a whiteboard/doodle animation explainer sequence for CyberShield Alerts. You must split the topic/story into EXACTLY 8 to 10 logical slides (scenes).

Given the article below, output STRICT JSON in exactly this shape:
{
  "slides": [
    {
      "type": "whiteboard",
      "title": "...",
      "text": "...",
      "doodleType": "...",
      "accentColor": "...",
      "imageQuery": "..."
    }
  ]
}

Field rules for each slide:
- "type": MUST be "whiteboard".
- "title": A very short, punchy title/headline for the step (2-5 words, e.g. "Step 1: The Phishing Mail" or "How it works").
- "text": A short, clear sentence (8-12 words) describing what is happening in this step. Keep it simple and direct.
- "doodleType": Choose EXACTLY one of these supported types:
  * "confused_user": a stick figure scratching head / confused (e.g. for general questions, user confusion, or the initial hook)
  * "hacker": a stick figure in a black hoodie / typing on laptop (e.g. for hacker activities, threat actors, phishing launch)
  * "server_network": stack of server boxes connected in a network (e.g. for networks, data streams, cloud setup)
  * "secured": a shield with a checkmark or lock (e.g. for encryption, security fixes, safe systems, patching)
  * "attack": a computer monitor with lightning bolt/warning (e.g. for exploits, cyber attacks, malware execution)
  * "cloud_database": cloud connected to a database (e.g. cloud storage, database leaks, SaaS)
  * "ransomware": locked screen with a sad stick figure (e.g. ransomware, locked files, extortion)
  * "phishing": envelope hanging on a fish hook (e.g. phishing emails, fake links, social engineering)
  * "explainer": stick figure holding a pointer stick pointing to a board (e.g. teaching a concept, explaining mechanism)
  * "summary": checklist/checkmarks (e.g. final takeaway, checklist, recap slide)
- "accentColor": Choose EXACTLY one of: "blue" (for informational/secured/cloud), "orange" (for warnings/hackers/phishing), or "red" (for critical attacks/ransomware/exploits).
- "imageQuery": A short search term (3-6 words) representing the scene, as a fallback (e.g. "confused person computer", "hacker coding").

Output ONLY the JSON, no other text.

Article:
{{ARTICLE_TEXT}}
