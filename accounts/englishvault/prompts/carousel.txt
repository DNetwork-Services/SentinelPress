You are writing the slide-by-slide text for a The English Vault Instagram carousel (5-7 slides).

Given the lesson content below, output STRICT JSON in this shape:
{
  "slides": [
    { "type": "title", "text": "...", "imageQuery": "..." },
    { "type": "body", "heading": "...", "text": "...", "hindiMeaning": "...", "imageQuery": "..." },
    { "type": "comparison", "wrong": "...", "right": "...", "imageQuery": "..." },
    { "type": "cta", "text": "...", "imageQuery": "..." }
  ]
}

Slide types to use:
- Slide 1 is always "title" — a curiosity-driven or slightly provocative headline calling out a common mistake (e.g. "You're probably saying this idiom wrong")
- One "body" slide should give the meaning/definition — this slide MUST include "hindiMeaning": a natural, accurate Hindi translation of the word/idiom/concept in Devanagari script (not transliteration) so Hindi-speaking learners immediately understand it. Other body slides (usage tips, example sentences) can omit "hindiMeaning".
- If the lesson content mentions a common mistake, include exactly one "comparison" slide: "wrong" is the incorrect usage (as a learner might actually write/say it), "right" is the correct version — both as short, realistic example sentences, not abstract descriptions
- Final slide is always "cta" — inviting, encouraging tone
- Stay strictly grounded in the lesson content's facts — no invented rules, definitions, translations, or examples

For "imageQuery" on EVERY slide, write a short stock-photo search phrase (3-6 words) matching that slide's mood:
- Title/hook slide → curious or intriguing
- The "comparison" slide → relatable, light-hearted mix-up moment
- The meaning/definition slide → clear, thoughtful
- An example-sentence slide → everyday scene matching the example's context
- The CTA slide → warm, inviting, aspirational

Each slide's imageQuery should be DIFFERENT and specific — avoid reusing the same query across slides.

Output ONLY the JSON, no other text.

Lesson content:
{{ARTICLE_TEXT}}
