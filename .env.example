# This file is a reference only — never commit real secrets.
# In production (GitHub Actions), all of these live in GitHub Secrets instead.

# --- Per-account Instagram credentials (names must match config.json's envVar fields) ---
CYBERSHIELDALERTS_IG_ACCESS_TOKEN=
CYBERSHIELDALERTS_IG_BUSINESS_ID=

# --- Telegram (shared across accounts) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- LLM providers (Milestone 3) ---
GEMINI_API_KEY=
GROQ_API_KEY=
